package geometries;

import org.junit.jupiter.api.Test;
import primitives.Point3D;

import static org.junit.Assert.fail;


class SphereTest {

    @Test
    void testGetNormal() {
        Sphere sphere2 = new Sphere(new Point3D(0, 1, 1), 3);
        /** even if the radius is 0 */
        fail("to be done");
    }
}